# Local dev — copy to .env and load in IDE run configuration.
# Infra: make dev (postgres + migrate)

DATABASE_URL=postgres://brick:brick@localhost:5432/brick?sslmode=disable

HTTP_ADDR=:8080
ADMIN_HTTP_ADDR=:9090

# Optional admin token for /ready and /metrics
# ADMIN_HTTP_TOKEN=
